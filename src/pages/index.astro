---
import MainLayout from '../layouts/MainLayout.astro';
import {
  myName,
  myBio,
  navLinks,
  selectedProjects,
  aboutMe,
  skills,
  workExperiences,
  writingText,  // comment out to remove blog
  contactText,
  contactOptions,
} from '../data';
import AboutSection from '../components/sections/AboutSection.astro';
import MainSection from '../components/sections/MainSection.astro';
import BlogSection from '../components/sections/BlogSection.astro'; // comment out to remove blog
import Footer from '../components/Footer.astro';
import ContactSection from '../components/sections/ContactSection.astro';
---

<MainLayout>
  <MainSection
    navLinks={navLinks}
    myName={myName}
    myBio={myBio}
    selectedProjects={selectedProjects}
  />
  <AboutSection
    aboutMe={aboutMe}
    skills={skills}
    workExperiences={workExperiences}
  />
  <BlogSection writingText={writingText} />
  <ContactSection contactText={contactText} contactOptions={contactOptions} />
  <Footer navLinks={navLinks} />
</MainLayout>
